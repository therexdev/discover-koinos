# secrets — never commit keys
gateway.env
*.env
data/token-keys.json

# runtime state
data/*.json
!data/.gitkeep

# deps & builds
node_modules/
contracts/node_modules/
contracts/token/build/
contracts/token/asconfig.json

# uploaded NFT images (runtime, may be large)
data/uploads/
data/logins.json
data/collection-keys.json
